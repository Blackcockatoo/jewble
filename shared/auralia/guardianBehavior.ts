// Re-export the JSX-powered guardian behaviors so consumers pulling the .ts
// path still receive the real implementation.
export * from './guardianBehavior.tsx';
export { default } from './guardianBehavior.tsx';
